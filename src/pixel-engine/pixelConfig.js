export const ABOUT_PHOTO_PIXEL = {
  preset: "default",
  fromPixels: 40,
  speed: 70,
  threshold: 0.2,
  replay: false,
  hoverPause: false,
  observe: false,
}

export const PROJECT_SCREENSHOT_PIXEL = {
  preset: "default",
  fromPixels: 40,
  speed: 90,
  threshold: 0.15,
  replay: false,
}
